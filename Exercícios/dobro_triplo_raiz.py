# Programa para calcular o dobro, triplo e raiz quadrada de um número.

numero = int(input('Digite um número para calculos: '))
print('Dobro: {}, triplo: {} e raiz quadrada: {}'.format(numero*2, numero*3, int(numero**0.5)))
